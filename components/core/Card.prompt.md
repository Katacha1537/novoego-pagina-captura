Dark warm surface with hairline + bevel; holds grouped content.

```jsx
<Card variant="feature">
  <h3 style={{fontFamily:'var(--font-editorial)'}}>Páginas de Venda Persuasivas</h3>
  <p className="t-body">Design + copywriting de alta conversão.</p>
</Card>
<Card variant="quote">"Comunicação correta + design poderoso = combinação imbatível."</Card>
```

Variants: `default`, `feature` (bigger radius/padding), `quote` (gold left rule). Set `interactive` for hoverable grid cards.
